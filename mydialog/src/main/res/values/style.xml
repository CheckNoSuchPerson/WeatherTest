<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="dialog_from_bottom_anim">
        <item name="android:windowEnterAnimation">@animator/anim_bottom_in</item>
        <item name="android:windowExitAnimation">@animator/anim_bottom_out</item>
    </style>

    <style name="dialog_style" parent="android:Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否背景模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置背景模糊的透明度-->
        <item name="android:backgroundDimAmount">0.5</item>
        <!--背景的圆角-->
        <item name="android:background">@drawable/dialog_drawable</item>
    </style>
</resources>